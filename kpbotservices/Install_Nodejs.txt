1. use curl to retrieve the installation script for your preferred version

   sudo apt-get install curl

2. $ curl -sL https://deb.nodesource.com/setup_12.x | sudo -E bash -

3. $ sudo apt-get install nodejs

4. To check which version of Node.js

   $ nodejs -v

   {
       if node version show less than '10'.
       1.sudo npm install n -g.
       2.sudo n latest
       3. Check version of Node.Js using
          $ nodejs -v
   }

5. Check version of npm

   $ npm -v

6. If npm is not installed

   $ sudo apt-get install npm

7. Check the version of npm

   $ npm -v

8. Then go to 'kpbotservices' directory run

   1. $ sudo npm install -g nodemon , Then
   2. npm install

9. Then

   $ nodemon
